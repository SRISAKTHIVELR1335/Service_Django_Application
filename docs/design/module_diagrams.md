# Module Diagrams

Include diagrams (Mermaid, PlantUML, or images) showing:
- Backend module boundaries
- Windows client layers (UI, runtime, drivers)
- Android app modules (screens, navigation, API)
